import { MealFilterPipe } from './meal-filter.pipe';

describe('MealFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MealFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
